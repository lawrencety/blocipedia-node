<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../static/partials/head.ejs %>
  </head>
  <body>
    <% include ../static/partials/navbar.ejs %>
    <main class="container">
      <h2>Edit Wiki</h2>
      <form action="/wiki/<%= wiki.id %>/update" method="post">
        <div class="form-group">
          <label for="title">Wiki title</label>
          <input type="text" class="form-control" value="<%= wiki.title %>" name="title" aria-describedby="titleHelp" placeholder="<%= wiki.title %>">
          <small class="text-muted" id="titleHelp">title must be 2 or more characters in length.</small>
        </div>
        <div class="form-group">
          <label for="body">Wiki body</label>
          <textarea name="body" class="form-control" placeholder="Enter Wiki body" rows="6" cols="60"><%= wiki.body %></textarea>
          <small class="text-muted" id="bodyHelp">body must be 10 or more characters in length.</small>
        </div>
        <% if (currentUser.role !== 0) { %>
          <div class="form-group">
            <label for="private">Private?</label>
            <% if (wiki.private) { %>
              <input type="checkbox" name="private" checked>
            <% } else { %>
              <input type="checkbox" name="private">
            <% } %>
          </div>
        <% } %>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </main>
    <% include ../static/partials/baseScripts.ejs %>
  </body>
